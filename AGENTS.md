# Codex 仓库规则

## Skill、MCP、App 与插件调用授权

### 默认规则

- 默认禁止隐式或自动使用任何 skill、MCP tool、App/Connector 或插件能力。
- 仅仅因为任务语义与某项能力的 description 匹配，不构成使用授权。
- 某项能力已安装、已连接、当前可用、过去曾使用过，或 Codex 认为它可能有帮助，都不构成授权。
- 用户只要求普通分析、写作、搜索、编码或文件修改时，使用基础能力完成，不得自动升级为 skill、MCP、App 或插件工作流。

### 授权条件

仅在以下任一条件成立时，才视为用户授权使用某项具体能力：

1. 用户在当前消息中显式点名并要求使用该能力；
2. Codex 先说明具体能力、用途、必要性和对当前任务的影响，用户随后明确确认。

可识别的显式授权包括：

- Skill：`/skill-name`、`$skill-name` 或“使用 skill-name skill”。
- MCP：“使用 xxx MCP”、“调用 xxx MCP/tool”或其他明确指向具体 MCP 工具的表述。
- App/Connector：用户点名并要求使用具体 App、Connector，或在消息中附加对应 App。
- 插件：用户点名并要求使用具体插件或其提供的具体能力。

### 授权范围

- 一次授权只适用于当次明确说明的能力和任务范围。
- 对一项能力的授权不自动授权其他 skill、MCP、App、Connector 或插件。
- 授权不自动延续到后续独立任务。
- 需要新增、替换或组合其他能力时，必须再次说明并等待用户确认。

### 禁止行为与冲突处理

- 未获授权时，不得读取对应 `SKILL.md`，不得调用对应 MCP tool、App/Connector 或插件能力。
- 不得因为未授权能力中的强制工作流，向用户提出档位、模式、分类或流程选择问题。
- 可以告知用户某项能力可能适用，但必须等待用户确认后才能加载或调用。
- 如果任务确实依赖某项未授权能力才能继续，应说明原因并请求授权，不得静默调用。
- 如果更高优先级的系统或平台规则强制要求使用某项能力，应先向用户说明冲突及其影响，不得静默调用。
