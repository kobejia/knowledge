# Codex 仓库规则

## Skill 调用授权

- 未经用户授权，不得使用任何 skill。
- 以下任一条件成立时，才视为用户授权使用某个 skill：
  1. 用户显式调用该 skill，例如 `$personal-learn`；
  2. Codex 先说明具体 skill、用途和对当前任务的影响，用户随后明确确认。
- 不得仅因为任务与 skill 描述匹配、skill 已安装、过去曾使用过，或 Codex 认为它可能有帮助而自动启用。
- 一次授权只适用于当次明确说明的 skill 和任务范围，不自动授权其他 skill，也不延续到后续独立任务。
- 需要新增、替换或组合其他 skill 时，必须再次说明并等待用户确认。
- 未获得授权时，使用普通能力继续完成任务；如果任务确实依赖某个 skill 才能继续，应说明原因并请求授权，不得静默调用。
