# Chrome Extension 架构核心

```infographic
infographic list-grid-badge-card
theme
  palette #b7472a #27647a #4e722e #a56a13
data
  title Chrome Extension 架构核心
  desc Manifest V3 下的四个关键设计
  items
    - label 权限最小化
      desc 从用户动作反推 API 与 host 范围
    - label 消息协议化
      desc 验证低信任输入，只暴露固定能力
    - label 状态可恢复
      desc Service Worker 可终止，真相必须持久化
    - label 特权隔离
      desc Content Script 靠近页面，不得成为特权中心
```
